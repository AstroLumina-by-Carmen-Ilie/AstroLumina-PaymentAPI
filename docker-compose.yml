services:
  payment:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/astrolumina-by-carmen-ilie/astrolumina-paymentapi:latest
    pull_policy: always
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3032:3032"
    healthcheck:
      test: [ "CMD", "wget", "--spider", "-q", "http://localhost:3032/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.125'
          memory: 128M
    networks:
      - payment-api

networks:
  payment-api:
    name: payment-api-network
    driver: bridge
